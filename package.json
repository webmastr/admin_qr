{
  "name": "my-full-stack-project",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "start npm run frontend && start npm run backend",
    "frontend": "npm run dev --workspace=frontend",
    "backend": "npm run dev --workspace=backend",
    "lint": "npm run lint --workspace=frontend"
  }
}
